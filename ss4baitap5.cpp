#include<stdio.h>
int main(){
	int so1, so2, so3;
	scanf("%d", &so1);
	scanf("%d", &so2);
	scanf("%d", &so3);
	if(so1 < so2 && so2 < so3){
		printf("so thu hai nam giua hai so con lai");
	}
	return 0;
}
